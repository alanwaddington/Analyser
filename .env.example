# Upstash Redis — required for cross-device label sync (issue #78)
# Obtain from Vercel Marketplace → Upstash Redis integration
# Also set these in .env.local for local development
UPSTASH_REDIS_REST_URL=
UPSTASH_REDIS_REST_TOKEN=
